spring:
  application:
    name: dragos-service
  jpa:
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: false
    generate-ddl: false
    database: mysql
    open-in-view: false
  flyway:
    enabled: true
    baseline-on-migrate: true
  datasource:
    url: jdbc:mysql://localhost:3306/sportssocialmediadb
    username: root
    password: root
    hikari:
      maximum-pool-size: 10